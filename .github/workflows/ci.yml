name: CI Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      FAKE_OPENAI: true
      INTERNAL_API_KEY: staging-api-key-721

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m venv .venv
          source .venv/bin/activate
          pip install --upgrade pip
          pip install -r requirements-dev.txt

      - name: Run linters (black, isort, flake8, mypy)
        run: |
          black . --check
          isort . --check-only
          flake8 .
          mypy .

      - name: Run tests
        run: |
          pytest tests
